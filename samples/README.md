# Samples

This directory holds audio recordings for testing.

- **Committed**: Only synthetic test fixtures (< 1 MB each)
- **Gitignored**: All real audio files (*.m4a, *.mp3, *.wav, etc.)

Place your real lecture recordings here. They will not be committed to git.
